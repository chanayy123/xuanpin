# Windows 双击启动器

双击根目录的 `启动最新版选品站.bat`，程序会：

1. 查找 `feature/catalog-sync-dashboard` 分支最新一次成功的 GitHub Actions 任务。
2. 下载并校验其中的 `xuanpin-dashboard-*` 构建产物。
3. 在本机 `127.0.0.1:4173` 启动静态服务器。
4. 自动使用默认浏览器打开选品站。

首次使用要求：

- 安装 Node.js 20 或更高版本。
- 安装 [GitHub CLI](https://cli.github.com/)。
- 按启动器提示完成一次 GitHub 网页登录；后续无需重复登录。

下载目录、PID 和日志文件都位于项目目录内，并已加入 `.gitignore`。启动器只监听本机回环地址，不会向局域网公开网页。

> GitHub 的定时工作流只从默认分支读取。当前新实现保存在功能分支，因此首次推送会生成构建产物；要启用每日 03:15 的持续同步，需要之后合并到默认分支，或将该功能分支设置为默认分支。
